import { BaseService } from './base.service';

/**
 * Student Service Layer
 * 
 * Purpose: Business logic for student-facing features.
 * Responsibilities:
 * - Fetching scholarships (MongoDB).
 * - Tracking application status (PostgreSQL).
 * - Student profile CRUD (PostgreSQL).
 */

export class StudentService extends BaseService {
  // Logic for students
}
